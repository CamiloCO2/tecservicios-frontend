import {loadServices} from '../core/api.js';import {getParam} from '../core/format.js';export async function initDetalle(){const box=document.querySelector('#detalle-servicio');if(!box)return;const id=getParam('id');const services=await loadServices();const s=services.find(x=>String(x.id)===String(id));if(!s){box.innerHTML='<p class="muted">Servicio no encontrado.</p>';return}box.innerHTML=`<a href="servicios.html">← Volver al catálogo</a><div class="detail"><div><img alt="${s.nombre}" src="${s.imagen}"/></div><div><h2>${s.nombre}</h2><div class="muted">Categoría: ${s.categoria}</div><p class="price" style="margin:.4rem 0 1rem">${new Intl.NumberFormat('es-CO',{style:'currency',currency:'COP',maximumFractionDigits:0}).format(s.precio)}</p><p>${s.descripcion}</p><p class="muted">Cantidad disponible: ${s.cantidad}</p><div class="qty"><label for="qty">Cantidad</label><input id="qty" type="number" min="1" value="1" /><button id="btn-solicitar" class="btn btn-primary">Solicitar servicio</button></div></div></div>`;document.querySelector('#btn-solicitar')?.addEventListener('click',()=>{const q=parseInt(document.querySelector('#qty').value||'1',10);alert(`Has solicitado ${q} × ${s.nombre}. (Demo sin backend)`)})}