import {loadServices} from '../core/api.js';import {createServiceCard} from '../core/dom.js';export async function initHome(){const grid=document.querySelector('#destacados');if(!grid)return;const services=await loadServices();const destacados=services.filter(s=>s.promo).slice(0,4);const pick=destacados.length?destacados:services.slice(0,4);pick.forEach(s=>grid.appendChild(createServiceCard(s)))}