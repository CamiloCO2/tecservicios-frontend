import {login} from '../core/store.js';export function initLogin(){const form=document.querySelector('#form-login');if(!form)return;form.addEventListener('submit',e=>{e.preventDefault();const email=form.email.value.trim();const pass=form.password.value.trim();if(email==='admin@tecservicios.com'&&pass==='123456'){login();location.href='dashboard.html'}else{alert('Credenciales inválidas')}})}