export function initContacto(){const form=document.querySelector('#form-contacto');if(!form)return;const msg=document.getElementById('contacto-msg');form.addEventListener('submit',e=>{e.preventDefault();const data=Object.fromEntries(new FormData(form).entries());if(!data.nombre||!data.email||!data.asunto||!data.mensaje){msg.textContent='Por favor completa todos los campos.';msg.style.color='#f1c40f';return}const emailOk=/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(data.email);if(!emailOk){msg.textContent='Ingresa un email válido.';msg.style.color='#f1c40f';return}const list=JSON.parse(localStorage.getItem('contactMessages')||'[]');list.push({...data,fecha:new Date().toISOString()});localStorage.setItem('contactMessages',JSON.stringify(list));msg.textContent='¡Mensaje enviado! Te contactaremos pronto.';msg.style.color='#2ecc71';form.reset()})}